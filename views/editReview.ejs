<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/css/styles.css">
    <title>Review Box App</title>
</head>

<body>
    <% include ('partials/navbar.ejs') %>

    <div class="container">
        <br>
        <div class="text-center">
            <h2>Edit Review</h2>
        </div>
        <br>
        <div class="text-center bg-dark">
            <% if (review) { %>
            <form action="/editReview/<%= review.reviewId %>" method="POST">
                <div class="mb-3">
                    <label for="accountName">Account Name:</label><br>
                    <input type="text" id="accountName" name="accountName" value="<%= review.accountName %>"
                        required><br><br>
                </div>
                <div class="mb-3">
                    <label for="rating">Rating:</label><br>
                    <input type="number" id="rating" name="rating" min="1" max="5" value="<%= review.rating %>"
                        required><br><br>
                </div>
                <div class="mb-3">
                    <label for="review">Review:</label><br>
                    <textarea id="review" name="review" rows="4" cols="50" required><%= review.review %></textarea><br><br>
                </div>
                <button type="submit">Update Review</button>
            </form>
            <% } else { %>
            <p>No review found.</p>
            <% } %>
            <a href="/movie/<%= review.movieId %>">Back to Review List</a>
        </div>
    </div>
</body>

</html>
