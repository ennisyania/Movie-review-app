<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/css/styles.css">
    <title>Movie Box App</title>
</head>

<body>
    <%- include ('partials/navbar.ejs') %>
        <div class="movie-container">
            <div class="poster">
                <img src="/images/<%= movie.image %>" alt="<%= movie.name %>">
            </div>
            <div class="details">
                <h1>
                    <%= movie.name %>
                        </h2>
                        <p>
                            <%= movie.description %>
                        </p>
                        <!-- Edit Link -->
                        <a href="/editMovie/<%= movie.movieId %>">Edit</a></td>
                        <!-- Delete Link -->
                        <a href="/deleteMovie/<%= movie.movieId %>"
                            onclick="return confirm('Are you sure you want to delete this movie?')">Delete</a></td>

            </div>
        </div>
        <div class="movie-container">
            <div class="details">
                <h1>
                    Reviews
                </h1>
                <table class="table table-hover small text-center table-dark">
                    <thead>
                        <tr>
                            <th width="150">Account Name</th>
                            <th>Rating</th>
                            <th>Review</th>
                            <th width="50">Edit</th>
                            <th width="50">Delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(let i=0; i < review.length; i++) { %>
                            <tr>
                                <td>
                                    <%= review[i].accountName %>
                                </td>
                                <td>
                                    <%= review[i].rating%>/5
                                </td>
                                <td>
                                    <%= review[i].review%>
                                </td>

                                <!-- Edit Link -->
                                <td><a href="/editReview/<%= review[i].reviewId %>">Edit</a></td>
                                <!-- Delete Link -->
                                <td><a href="/deleteReview/<%= review[i].reviewId %>"
                                        onclick="return confirm('Are you sure you want to delete this review?')">Delete</a>
                                </td>
                            </tr>
                            <% } %>
                    </tbody>
                </table>
                <a href="/addReview/<%= movie.movieId%>">Add Review</a>


            </div>
        </div>
</body>

</html>