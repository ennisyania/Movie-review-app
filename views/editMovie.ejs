<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/css/styles.css">
    <title>Movie Box App</title>
</head>

<body>
    <%- include ('partials/navbar.ejs') %>

    <div class="container">
        <br>
        <div class="text-center">
            <h2>Update Movie Details</h2>
        </div>
        <br>
        <div class="text-center bg-dark">
            <% if (movie) { %> 
            <form action="/editMovie/<%= movie.movieId %>" method="POST" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="name">Movie Name:</label><br>
                    <input  type="text" id="name" name="name" value="<%= movie.name %>" required><br><br>
                </div>
                <div class="mb-3">
                    <label for="description">Description:</label><br>
                    <textarea id="description" name="description" rows="4" cols="50" required><%= movie.description %></textarea><br><br>
                </div>
                <div class="mb-3">
                    <label for="currentImage">Current Image:</label><br>
                    <input type="text" name="currentImage" value="<%= movie.image %>" style="color: black;" disabled><br>
                    <img src="/images/<%= movie.image %>" width="20%"><br><br>
                </div>
                <div class="mb-3">
                    <label for="newImage">New Image:</label><br>
                    <input type="file" id="newImage" name="image" accept="image/*" ><br><br>
                </div>
                <button type="submit">Update Movie</button>
            </form>
            <% } else { %>
            <p>No movie found.</p>
            <% } %>
            <a href="/">Back to Movie List</a>
        </div>
    </div>
</body>

</html>
